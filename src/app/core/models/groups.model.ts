export interface Group {
    id: number;
    grade: number;
    shift: string,
    group: string,
    subject: string
    teacher: string,
    specialty: string,
    selected: boolean,
    name: string
}